# Architecture Gate — 架构一致性检查

> 始终启用。在代码审查前自动触发，防止每次改动偏离架构基线，积累技术债。

## 核心原则

**diff 级别的审查只看代码本身，看不到架构。** 多模块迭代时，每个功能都"局部最优"，但合在一起架构就烂了。

architecture-gate 在 `/review` 之前设置一道门槛，只问三个问题，不让 AI 审查自己看不懂的东西。

## 三问检查

### 问1: 模块边界 — 这次改动有没有破坏隔离？

```
检查项：
- 新增的 import 是否跨越了模块边界？
  （例如 ModuleA 的 service 直接 import 了 ModuleB 的 repository）
- 是否绕过了已有的 API/接口，直接访问了内部实现？
- 有没有把应该属于另一个模块的代码写在了当前模块？

做法：
- 查看 diff 中所有新增的 import 语句
- 对照项目中已有的模块划分（从目录结构推断）
- 标记任何可疑的跨模块引用
```

### 问2: 依赖方向 — 新增的依赖是否合理？

```
检查项：
- 低层代码是否 import 了高层代码？（依赖倒置）
  （例如 domain/entity import 了 infra/database）
- 是否引入了循环依赖？
  （A import B，B import A）
- 是否引入了新的外部依赖？
  （新增了 npm package，有没有必要？有没有更轻的替代？）

做法：
- 检查 diff 中新增的 import 方向
- 对照项目的分层约定（从现有代码推断）
- 新引入的外部依赖 → 标记为需要人工确认
```

### 问3: 模式一致性 — 新增代码和其他模块写法一样吗？

```
检查项：
- 其他模块用 Repository 模式，这次是否也用了？
- 其他模块的错误处理用 try-catch + logger，这次是否一致？
- 其他模块 API 返回格式统一（envelope），这次是否也遵循？
- 文件命名是否和项目约定一致？

做法：
- 随机抽查 1-2 个相邻模块的同类型文件
- 对比新增代码的结构和模式
- 不一致 → 标记，建议对齐
```

## 触发条件

在以下时间点自动触发：
1. **层5 TDD 编码完成后**（所有 P0 任务完成时）
2. **层8 /review 之前**（作为 /review 的前置检查）

## 输出格式

```
## Architecture Gate 检查报告

### 模块边界
- ✅ 无跨模块直接调用
- ⚠️ UserService 直接 import 了 PaymentModule 的 PaymentRepository
  建议: 通过 PaymentService 的公开接口访问

### 依赖方向
- ✅ 依赖方向合理
- ⚠️ 新增依赖: "axios" (1.2MB)
  确认: 项目中已有 "fetch" 可用，是否必须引入 axios？

### 模式一致性
- ✅ 错误处理模式与其他模块一致
- ⚠️ 新增 Controller 未使用统一的 ApiResponse envelope
  建议: 对齐其他 Controller 的返回格式

### 总结
- 通过: 7 项
- 警告: 3 项（建议在 /review 前处理）
- 阻塞: 0 项
```

## 与其他工具的配合

| 工具 | 检查范围 | 时机 |
|------|---------|------|
| architecture-gate | 模块边界 / 依赖方向 / 模式一致性 | /review 之前 |
| gstack /review | SQL / 信任边界 / 副作用 / 错误处理 | architecture-gate 之后 |
| gstack /cso | 密钥 / 供应链 / OWASP | /review 之后 |

## 反模式

- ❌ 跳过就直接 /review — arch-gate 是强制性门槛
- ❌ 对警告视而不见 — 警告今天不处理，下周就是技术债
- ❌ 审查自己写的新模块 — 对照的是"项目已有模式"，不是"这个 PR 的模式"
