# TodoList Management — 阶段追踪与中断恢复

> 始终启用。确保 AI 在写作何代码前知道当前进度，层与层之间不跳步，会话中断后可无缝恢复。

## 核心原则

**todolist.md 是 vibe coder 和 AI 之间的进度契约。** 它同时追踪两个维度：

1. **阶段进度** — 9 层工作流走到了哪一步
2. **任务进度** — 当前阶段有哪些具体任务

## 自动生成

### 触发条件

当以下任一事件发生时，自动生成/更新 `todolist.md`：

1. **/office-hours 完成** — 初始化 todolist，标记层1完成，层2待开始
2. **/opsx:propose 完成** — 标记层2完成，层3待开始
3. **superpowers writing-plans 完成** — 标记层4完成，同步任务列表
4. **用户说"开始做 XX 功能"** — 如果还没有 todolist，自动创建

### 固定格式

```markdown
# TodoList — <项目名>

> 当前阶段: 层3 方案设计
> 最后更新: YYYY-MM-DD HH:MM

## 🗺️ 阶段追踪（9 层工作流）

- [x] 层1: /office-hours — 需求已验证
- [x] 层2: /opsx:propose — 规格已完成
- [ ] 层3: brainstorming — 方案设计 ← 当前
- [ ] 层4: writing-plans — 计划拆分
- [ ] 层5: TDD + subagent-dev — 编码测试
- [ ] 层6: /browse + /qa — 浏览器验证
- [ ] 层7: /review + /cso — 审查安全
- [ ] 层8: /ship — 发布
- [ ] 层9: /opsx:archive — 归档

## 📋 P0 任务（当前必须完成）

- [ ] 任务描述（简短，一句话能看懂）
- [x] 已完成的任务
- [ ] 🔴 阻塞的任务 — 原因：等待后端 API 部署

## 📋 P1 任务（后续做）

- [ ] 优化项、增强项
```

### 格式规则

- P0 任务数 ≤ 7 个
- 每个任务一行 checkbox
- 🔴 标记阻塞项并注明原因
- 每次更新时刷新"最后更新"和"当前阶段"
- 阶段之间的边界：**上层未勾选完成，AI 禁止开始下层**

## 层间流转规则（强制）

```
┌───────────────────────────────────────────────────┐
│  AI 在做任何事之前，必须先读 todolist.md           │
│  检查"当前阶段"：该阶段未完成 → 回到该阶段        │
│  想跳阶段 → 先确认上层已完成 → 更新阶段追踪 → 继续 │
└───────────────────────────────────────────────────┘
```

1. **新会话启动** → 读 todolist.md → 找到"当前阶段" → 从该阶段开始
2. **阶段完成** → 勾选该层 [] → [x] → 更新"当前阶段"为下一层
3. **上层未完成** → 禁止跳到下层。必须回到上层完成后再前进
4. **部分层可跳过**（层6非前端项目跳过、层7非敏感模块跳过）→ 直接标记 [x] skip

## 自动更新

### 阶段完成时

```
- [ ] 层3: brainstorming — 方案设计 ← 当前
    ↓
- [x] 层3: brainstorming — 方案设计
- [ ] 层4: writing-plans — 计划拆分 ← 当前
```

### 任务完成时

```
- [ ] 实现深色模式切换  →  - [x] 实现深色模式切换
```
同时更新"最后更新"时间戳。

### 所有 P0 完成时

- 提醒用户运行 `/opsx:archive` 归档
- 询问是否将 P1 提升为 P0

## 会话恢复

用户说"继续"、"接着做"时：
1. 读 `todolist.md` → 找到"当前阶段"
2. 如果有未勾选的 P0 → 从第一个未勾选开始
3. 如果阶段追踪有未完成 → 回到该阶段

## 与其他工具的配合

| 阶段 | 工具 | todolist 行为 |
|------|------|-------------|
| 层1 | gstack /office-hours | 完成后更新阶段追踪 |
| 层2 | /opsx:propose | 完成后更新阶段追踪 |
| 层3 | superpowers brainstorming | 完成后更新阶段追踪 |
| 层4 | superpowers writing-plans | 完成后更新阶段追踪 + 同步 P0 任务 |
| 层5 | superpowers TDD + subagent-dev | 每完成一个任务标记 [x] |
| 层6 | gstack /browse + /qa | 完成后更新阶段追踪 |
| 层7 | gstack /review + /cso | 完成后更新阶段追踪 |
| 层8 | gstack /ship | 完成后更新阶段追踪 |
| 层9 | /opsx:archive | 完成后更新阶段追踪 + 归档 todolist 副本 |

## 反模式

- ❌ 不读就干：新会话不检查 todolist 直接写代码
- ❌ 跨层跳步：层4没完成就开始写代码
- ❌ 勾了没做：阶段标记 [x] 但实际没完成
- ❌ 阶段堆积：走到层5了层2还没勾
