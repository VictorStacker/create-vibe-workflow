# Grill Me — 想法追问

> 来源: Matt Pocock skills。在写规格前，把模糊想法问清楚。

## 核心原则

**vibe coder 最大的问题是需求太模糊。** "我想做个用户系统"——这不够。grill-me 在 `/opsx:propose` 之前自动触发，把模糊想法追问成具体需求。

## 工作方式

对用户的想法进行多轮追问，一次只问一个问题：

1. **逐层深入** — 不从第一个问题直接跳到最后一个
2. **问边界情况** — "如果用户忘了密码怎么办？"、"如果数据为空呢？"
3. **问优先级** — "其中哪个是你最需要的？"
4. **问约束** — "有时间限制吗？"、"需要支持手机吗？"
5. **问已有方案** — "你现在怎么解决这个问题的？"

## 追问维度

| 维度 | 示例问题 |
|------|---------|
| 功能范围 | "这个功能包含哪些操作？不包含哪些？" |
| 用户角色 | "谁会用到这个功能？不同角色看到的一样吗？" |
| 数据流向 | "数据从哪里来？需要持久化吗？" |
| 边界情况 | "如果出错怎么办？超时呢？并发呢？" |
| 优先级 | "其中哪个功能你最需要？哪些可以后续再做？" |
| 约束条件 | "有性能要求吗？需要支持多少用户？" |
| 已有方案 | "你现在怎么解决？有什么不满意的地方？" |

## 触发条件

在以下场景自动触发：
1. **用户描述新功能时** — 在 /opsx:propose 之前
2. **/office-hours 确认值得做之后** — 作为向规格化的过渡
3. **用户说"帮我想想"、"帮我捋捋"** — 手动触发

## 停止条件

当以下条件满足时，停止追问，输出总结：
- 用户能清晰回答"做什么、给谁用、怎么做"三个问题
- 追问了 5 轮以上，或用户说"可以了"

## 输出格式

追问结束后输出结构化总结：

```markdown
## 需求总结

### 做什么
[一句话描述核心功能]

### 用户是谁
[目标用户角色]

### P0（第一版必须做）
- [功能1]
- [功能2]

### P1（后续做）
- [功能3]

### 边界情况已确认
- [场景1]: [处理方式]
- [场景2]: [处理方式]

### 仍不明确（需用户确认）
- [问题1]
```

## 与其他工具的配合

```
/office-hours → "值不值得做？"
    ↓ 值得
grill-me → "具体怎么做？问清楚"
    ↓ 清晰了
/opsx:propose → "写成规格"
    ↓
brainstorming → "技术方案"
```

grill-me 负责"问清楚"——这是 /office-hours（验证真伪）和 /opsx:propose（写规格）之间的关键衔接。
