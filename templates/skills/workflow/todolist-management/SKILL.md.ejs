# TodoList Management — 进度追踪与中断恢复

> 始终启用。确保 AI 在写作何代码前知道当前进度，会话中断后可无缝恢复。

## 核心原则

**todolist.md 是 vibe coder 和 AI 之间的进度契约。** 它是项目根目录下一个简单的人类可读清单，任何人都能一眼看懂项目做到了哪里。

## 自动生成

### 触发条件

当以下任一事件发生时，自动生成/更新 `todolist.md`：

1. **superpowers writing-plans 完成** — 将计划中的任务提取为 checklist
2. **/opsx:propose 完成** — 将 tasks.md 中的任务同步到 todolist.md
3. **用户说"开始做 XX 功能"** — 如果还没有 todolist，自动创建

### 生成格式

```markdown
# TodoList — <项目名>

> 最后更新: YYYY-MM-DD HH:MM
> 当前状态: 🟢 进行中 / 🔴 阻塞 / ✅ 全部完成

## P0 — 当前必须完成

- [ ] 任务描述（简短，一句话能看懂）
- [x] 已完成的任务
- [ ] 🔴 阻塞的任务 — 原因：等待后端 API 部署

## P1 — 后续做

- [ ] 优化项、增强项

## 最近完成（本周）

- [x] 上周完成的任务 1
- [x] 上周完成的任务 2
```

### 格式规则

- P0 任务数 ≤ 7 个（超过说明拆分不够细）
- 每个任务一行，用 checkbox
- 🔴 标记阻塞项并注明原因
- 每次更新时刷新"最后更新"时间戳

## 会话恢复

### 新会话启动时

**第一步**：读取 `todolist.md`。
- 如果有未勾选的 P0 → 自动询问："检测到未完成任务 [任务名]，要接着做吗？"
- 如果全部完成 → 建议 `/opsx:archive` 或开始新功能

### 中断后继续

用户说"继续"、"接着做"、"继续上次的" 时：
1. 读 `todolist.md`
2. 读最近完成的 git commit（了解上次的代码状态）
3. 从第一个未勾选的 P0 任务开始

## 自动更新

### 任务完成时

每个任务完成后：
```markdown
- [ ] 添加登录页面  →  - [x] 添加登录页面
```
同时更新"最后更新"时间戳。

### 所有 P0 完成时

当 P0 全部勾选：
- 将 P0 标记为 `✅` 状态
- 提醒用户运行 `/opsx:archive` 归档
- 询问是否将 P1 提升为 P0

### 新任务加入时

用户说"再加一个 XX" 时，自动追加到对应优先级区。

## 与其他工具的配合

| 场景 | 工具 | todolist 角色 |
|------|------|-------------|
| 需求验证 | gstack /office-hours | — |
| 需求规格 | /opsx:propose | 从 tasks.md 初始化 |
| 计划拆分 | superpowers writing-plans | 从计划同步任务 |
| 编码执行 | superpowers subagent-driven-development | 每完成一个任务自动勾选 |
| 进度确认 | superpowers verification-before-completion | 检查 P0 是否全部完成 |
| 发布归档 | gstack /ship → /opsx:archive | 归档时将 todolist 迁移到 archive/ |

## 反模式

- ❌ 任务太模糊："完成前端" — 拆成"添加登录页"、"添加列表页"
- ❌ 任务太多：20 个 P0 — P0 不超过 7 个
- ❌ 不勾选：做了但没标记 — AI 必须主动勾选
- ❌ 不读就干：新会话直接写代码不检查 todolist — 必须先读
