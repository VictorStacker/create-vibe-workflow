<!-- WORKFLOW-START -->
<!-- 此区域由 create-vibe-workflow 自动生成 -->

## AI 协作工具链

**Codex 使用 `$skill-name` 触发技能。**

```
规划层（superpowers 等价）
  $brainstorming / $writing-plans / $tdd / $verify / $debug

执行层（gstack 等价）
  $office-hours / $browse / $qa / $review / $cso / $ship

衔接层（本工具）
  $opsx-propose（人话规格）/ $opsx-explore（探索）/ $opsx-archive（归档）
```

## 路由裁决表

| 任务 | 触发 |
|------|------|
| 验证需求 | `$office-hours` |
| 写需求规格 | `$opsx-propose` |
| 方案设计 | `$brainstorming` |
| 写实施计划 | `$writing-plans` |
| 写代码 | `$tdd` |
| 调试 | `$debug` |
| 完成前检查 | `$verify` |
| 代码审查 | `$review` |
| 安全审查 | `$cso` |
| 发布 | `$ship` |
| 归档 | `$opsx-archive` |
| 会话恢复 | 先读 `todolist.md` |

## 技术栈

- 语言: <%= LANGUAGE %>
- 项目类型: <%= PROJECT_TYPE %>
- 数据库: <%= NEEDS_DB === 'true' ? '是' : '否' %>

<!-- WORKFLOW-END -->
